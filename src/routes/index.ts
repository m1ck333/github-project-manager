/**
 * Routes Module
 * This module exports the main routing components and configurations
 */

// Export the main routes component
export { default as AppRoutes } from "./AppRoutes";

// Export route configuration
export { ROUTES, ROUTE_PATHS, NAV_GROUPS } from "./config/routes";
