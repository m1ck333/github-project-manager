#import "../fragments/collaboratorFields.graphql"

query GetRepositoryCollaborators($owner: String!, $name: String!) {
  repository(owner: $owner, name: $name) {
    id
    collaborators(first: 100, affiliation: ALL) {
      nodes {
        ...CollaboratorFields
      }
      edges {
        permission
      }
    }
  }
}
