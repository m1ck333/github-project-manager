# Mutation to update an existing GitHub Project (v2)
mutation UpdateProject($input: UpdateProjectV2Input!) {
  updateProjectV2(input: $input) {
    projectV2 {
      id
      title
      number
      url
      closed
      updatedAt
    }
  }
}
